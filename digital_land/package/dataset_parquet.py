# from .package import Package

# class DatasetParquet(Package):
#     """
#     Up until now we have primarily been storing and loading datasets
#     as sqllite. This package represents the same data but in a set
#     of parquet files.
#     """

#     def load(self,transformed_resource_dir):
#         """load data into the package, will replace the """

#         load_fact_resources()
#         load_facts
#         load_entity

#     def update()
